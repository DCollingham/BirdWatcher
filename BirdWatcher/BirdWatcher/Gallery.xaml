<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BirdWatcher.Gallery">
    <ContentPage.Content>
        <StackLayout>
            <CollectionView x:Name="collectionView">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout>
                            <Label Text="{Binding Name}"
                               FontSize="Medium" />
                            <Label Text="{Binding Location}"
                               TextColor="Silver"
                               FontSize="Small" />
                            <Label Text="{Binding ImageUrl}"
                               TextColor="Silver"
                               FontSize="Small" />
                            <Image Aspect="AspectFill"    
                                   HeightRequest="60"
                                   WidthRequest="60" 
                                   x:Name="ImageUrl" Source="{Binding ImageUrl}"/>

                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>